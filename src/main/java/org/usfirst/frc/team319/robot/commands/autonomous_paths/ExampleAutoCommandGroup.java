package org.usfirst.frc.team319.robot.commands.autonomous_paths;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class ExampleAutoCommandGroup extends CommandGroup {

	public ExampleAutoCommandGroup() {
/*
		addSequential(new ());
		addSequential(new ());
		addSequential(new ());
		
		*/

			}
		}
